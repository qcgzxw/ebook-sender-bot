[default]
mode=${APP_MODE}
email_send_limit=${MAX_SEND_LIMIT}
database=${DB}
format=${FORMAT}
email_provider=${EMAIL_PROVIDER}
vip=${VIP}

[mysql]
name=${DB_NAME}
host=${DB_HOST}
port=${DB_PORT}
user=${DB_USER}
password=${DB_PASSWORD}

[sqlite]
name=${DB_NAME}

[postgresql]
name=${DB_NAME}
host=${DB_HOST}
port=${DB_PORT}
user=${DB_USER}
password=${DB_PASSWORD}

[smtp]
host=${SMTP_HOST}
port=${SMTP_PORT}
username=${SMTP_USERNAME}
password=${SMTP_PASSWORD}

[provider]
mailcow_url=${MAILCOW_URL}
mailcow_api_key=${MAILCOW_API_KEY}
mailcow_mailbox_domain=${MAILCOW_MAILBOX_DOMAIN}

[telegram]
bot_token=${BOT_TOKEN}
developer_chat_id=${DEVELOPER_CHAT_ID}
